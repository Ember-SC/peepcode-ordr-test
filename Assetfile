# Assetfile
require 'rake-pipeline-web-filters'

# temp workaround to combile bug
# see: http://stackoverflow.com/questions/12866054/uninitialized-constant-rakepipelinemanifestentrydatetime
require 'fileutils'
FileUtils.rm_rf 'tmp'

output 'js/tests/generated'

input 'templates' do
    match '**/*.handlebars' do
        handlebars
        concat 'templates.js'
    end
end

# vim:ft=ruby